#include<stdio.h>

/**
 * main - Entry point
 * Return: 0 (success)
 */
int main(void)
{
	int digit = 48, n = 57;

	while (digit <= 56)
	{
		putchar(digit);
		putchar(',');
		putchar(' ');
		ch++;
	}

	putchar(n);
	return (0);
}
